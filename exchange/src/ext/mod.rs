//! Extension traits for exchange-specific functionality.
//!
//! The `DownloadRepository` trait is defined in `kairos-data` and implemented
//! by `DatabentoTradeRepository` in `repository::databento::trades`.
//!
//! This module exists as a placeholder for future exchange-specific extension
//! traits that don't belong in the core data layer.
