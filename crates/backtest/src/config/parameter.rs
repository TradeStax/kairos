//! Re-exports study parameter types so strategy authors only need to import from
//! `kairos_backtest::prelude`.
pub use kairos_study::{
    DisplayFormat, ParameterDef, ParameterKind, ParameterSection, ParameterTab, ParameterValue,
    StudyConfig, Visibility,
};
